import { Box } from "@chakra-ui/react";

const style = {
  height: "100vh",
  backgroundColor: "pink",
  minHeight: "800px",
  position: "relative",
};

const Resume = () => {
  return <Box sx={style}></Box>;
};

export default Resume;
