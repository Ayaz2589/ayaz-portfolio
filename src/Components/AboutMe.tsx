import { Box } from "@chakra-ui/react";

const style = {
  height: "100vh",
  backgroundColor: "aqua",
  minHeight: "800px",
  position: "relative",
};

const AboutMe = () => {
  return <Box sx={style}></Box>;
};

export default AboutMe;
